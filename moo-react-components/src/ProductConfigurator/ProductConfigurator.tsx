import React, { FC } from 'react';

export interface ProductConfiguratorProps {}

const ProductConfigurator: FC<ProductConfiguratorProps> = (props) => {
  return (
    <>
      <p>ProductConfigurator</p>
    </>
  );
};

export default ProductConfigurator;
